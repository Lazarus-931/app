[project]
name = "mlx-platform"
version = "0.1.0"
description = "Build tooling for a standalone mlx-vlm server distribution."
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py310"

